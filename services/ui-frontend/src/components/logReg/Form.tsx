import { type } from "os"
import * as React from "react"
import {Component, FunctionComponent} from "react"

type FormProps = {
    user_status: Boolean
}

export default function Form({
    user_status
}: FormProps){
    if(user_status){
        
    } else{
        
    }
    
}